# Project-level Codex config for the Amazon BSR weekly dashboard.
#
# Keep global defaults, auth, MCP servers, and plugin settings in:
#   the user-level Codex config outside this repository
#
# Project-specific skills live in:
#   .agents/skills
#
# This file is intentionally minimal so the project inherits user-level Codex
# configuration unless a future project-specific override is required.
